services:
  - type: web
    name: careconnect-server
    env: node
    plan: free
    buildCommand: chmod +x start.sh && ./start.sh
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
